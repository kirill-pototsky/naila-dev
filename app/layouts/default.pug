include _externalIncludes
include _internalIncludes

doctype html
html(lang='ru')
  include head
  body

    +header

    block content

    if !formNone
      +section#feedback
        .text-section
          if personalForm
            p Записаться на корпоративный
              br
              | тим-билдинг
          else
            p Записаться на пробное
              br
              | занятие
        +form#form(action="mail.php" method="post")
          //- +inputbox
          //-   +input(type="name" placeholder="Имя")
          +inputbox
            +input(type="text" name="Телефон" placeholder="Телефон" class="is-validate" required)
          +inputbox
            +checkradio(true, true) Я согласен на
              span #{' '} обработку персональных данных

          +button Получить бесплатное занятие

    +footer
    +copyright


    +modals
      +modal-form(modal-name="form")
    +modal-thanks
    +svg-gradient
    +preloader
    script(src=`assets/scripts/app.min.js`, charset='utf-8')
